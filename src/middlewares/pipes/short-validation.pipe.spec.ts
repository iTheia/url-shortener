import { ShortValidationPipe } from './short-validation.pipe';

describe('ShortValidationPipe', () => {
  it('should be defined', () => {
    expect(new ShortValidationPipe()).toBeDefined();
  });
});
